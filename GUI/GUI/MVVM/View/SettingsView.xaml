<UserControl x:Class="GUI.MWM.View.SettingsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:GUI.MWM.View"
             mc:Ignorable="d" 
             d:DesignHeight="600" d:DesignWidth="800"
             Background="#1b1b1b">

    <UserControl.Resources>
        <Style x:Key="CardStyle" TargetType="Border">
            <Setter Property="Background" Value="#252525"/>
            <Setter Property="CornerRadius" Value="8"/>
            <Setter Property="Padding" Value="15"/>
            <Setter Property="Margin" Value="0,0,0,15"/>
            <Setter Property="Effect">
                <Setter.Value>
                    <DropShadowEffect BlurRadius="10" ShadowDepth="2" Opacity="0.3"/>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="SectionHeaderStyle" TargetType="TextBlock">
            <Setter Property="Foreground" Value="#3498db"/>
            <Setter Property="FontSize" Value="18"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Margin" Value="0,0,0,10"/>
        </Style>

        <Style x:Key="LabelStyle" TargetType="TextBlock">
            <Setter Property="Foreground" Value="#CCCCCC"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="VerticalAlignment" Value="Center"/>
        </Style>

        <Style x:Key="ModernTextBox" TargetType="TextBox">
            <Setter Property="Background" Value="#333333"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="BorderBrush" Value="#444444"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Padding" Value="5"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="VerticalContentAlignment" Value="Center"/>
            <Setter Property="Height" Value="32"/>
        </Style>

        <Style x:Key="ActionButton" TargetType="Button">
            <Setter Property="Background" Value="#3498db"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Padding" Value="15,8"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" CornerRadius="4">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#2980b9"/>
                </Trigger>
            </Style.Triggers>
        </Style>
    </UserControl.Resources>

    <ScrollViewer VerticalScrollBarVisibility="Auto">
        <StackPanel Margin="20">
            <!-- Modalità di Funzionamento -->
            <Border Style="{StaticResource CardStyle}">
                <StackPanel>
                    <TextBlock Text="Modalità di Funzionamento" Style="{StaticResource SectionHeaderStyle}"/>
                    <UniformGrid Columns="4" Margin="0,5,0,0">
                        <RadioButton Content="Automatico" FontSize="16px" Foreground="White" IsChecked="{Binding IsAuto}" VerticalAlignment="Center"/>
                        <RadioButton Content="Manuale" FontSize="16px" Foreground="White" IsChecked="{Binding IsManual}" VerticalAlignment="Center"/>
                        <RadioButton Content="Senza Controlli" FontSize="16px" Foreground="White" IsChecked="{Binding IsNoControl}" VerticalAlignment="Center"/>
                        <RadioButton Content="Spento"  FontSize="16px" Foreground="White" IsChecked="{Binding IsOff}" VerticalAlignment="Center"/>
                    </UniformGrid>
                </StackPanel>
            </Border>

            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>

                <!-- Orari di Avvio -->
                <Border Style="{StaticResource CardStyle}" Grid.Column="0" Margin="0,0,10,15">
                    <StackPanel>
                        <TextBlock Text="Orari di Avvio" Style="{StaticResource SectionHeaderStyle}"/>
                        <Grid>
                            <Grid.RowDefinitions>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="Auto"/>
                            </Grid.RowDefinitions>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="Auto"/>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>

                            <TextBlock Text="Avvio 1:" Style="{StaticResource LabelStyle}" Grid.Row="0" Margin="0,5,10,5"/>
                            <TextBox Text="{Binding StartTime1}" Style="{StaticResource ModernTextBox}" Grid.Row="0" Grid.Column="1" Margin="0,5,0,5"/>

                            <TextBlock Text="Avvio 2:" Style="{StaticResource LabelStyle}" Grid.Row="1" Margin="0,5,10,5"/>
                            <TextBox Text="{Binding StartTime2}" Style="{StaticResource ModernTextBox}" Grid.Row="1" Grid.Column="1" Margin="0,5,0,5"/>
                        </Grid>
                    </StackPanel>
                </Border>

                <!-- Altre Impostazioni -->
                <Border Style="{StaticResource CardStyle}" Grid.Column="1" Margin="10,0,0,15">
                    <StackPanel>
                        <TextBlock Text="Sistema" Style="{StaticResource SectionHeaderStyle}"/>
                        <Grid>
                            <Grid.RowDefinitions>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="Auto"/>
                            </Grid.RowDefinitions>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="Auto"/>
                            </Grid.ColumnDefinitions>

                            <TextBlock Text="Pausa ciclo continuo:" Style="{StaticResource LabelStyle}" Grid.Row="0" Margin="0,5,10,5"/>
                            <TextBox Text="{Binding CycleTime}" Style="{StaticResource ModernTextBox}" Grid.Row="0" Grid.Column="1" Width="80" Margin="0,5,0,5"/>

                            <TextBlock Text="Sensore Pioggia:" Style="{StaticResource LabelStyle}" Grid.Row="1" Margin="0,5,10,5"/>
                            <CheckBox IsChecked="{Binding RainSensorActive}" Grid.Row="1" Grid.Column="1" VerticalAlignment="Center" HorizontalAlignment="Right"/>
                        </Grid>
                    </StackPanel>
                </Border>
            </Grid>

            <!-- Tempi di Irrigazione -->
            <Border Style="{StaticResource CardStyle}">
                <StackPanel>
                    <TextBlock Text="Tempi di Accensione" Style="{StaticResource SectionHeaderStyle}"/>
                    <UniformGrid Columns="2">
                        <Grid Margin="0,0,20,0">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="80"/>
                            </Grid.ColumnDefinitions>
                            <Grid.RowDefinitions>
                                <RowDefinition Height="40"/>
                                <RowDefinition Height="40"/>
                                <RowDefinition Height="40"/>
                            </Grid.RowDefinitions>

                            <TextBlock Text="Girandola G1:" Style="{StaticResource LabelStyle}" Grid.Row="0"/>
                            <TextBox Text="{Binding OnTime1}" Style="{StaticResource ModernTextBox}" Grid.Column="1" Grid.Row="0" Margin="0,4"/>

                            <TextBlock Text="Girandola G3A/B:" Style="{StaticResource LabelStyle}" Grid.Row="1"/>
                            <TextBox Text="{Binding OnTime2}" Style="{StaticResource ModernTextBox}" Grid.Column="1" Grid.Row="1" Margin="0,4"/>

                            <TextBlock Text="Girandola G2:" Style="{StaticResource LabelStyle}" Grid.Row="2"/>
                            <TextBox Text="{Binding OnTime3}" Style="{StaticResource ModernTextBox}" Grid.Column="1" Grid.Row="2" Margin="0,4"/>
                        </Grid>

                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="80"/>
                            </Grid.ColumnDefinitions>
                            <Grid.RowDefinitions>
                                <RowDefinition Height="40"/>
                                <RowDefinition Height="40"/>
                                <RowDefinition Height="40"/>
                            </Grid.RowDefinitions>

                            <TextBlock Text="Girandola G4:" Style="{StaticResource LabelStyle}" Grid.Row="0"/>
                            <TextBox Text="{Binding OnTime4}" Style="{StaticResource ModernTextBox}" Grid.Column="1" Grid.Row="0" Margin="0,4"/>

                            <TextBlock Text="Spruzzatore S1:" Style="{StaticResource LabelStyle}" Grid.Row="1"/>
                            <TextBox Text="{Binding OnTime6}" Style="{StaticResource ModernTextBox}" Grid.Column="1" Grid.Row="1" Margin="0,4"/>

                            <TextBlock Text="Spruzzatore S2:" Style="{StaticResource LabelStyle}" Grid.Row="2"/>
                            <TextBox Text="{Binding OnTime7}" Style="{StaticResource ModernTextBox}" Grid.Column="1" Grid.Row="2" Margin="0,4"/>
                        </Grid>
                    </UniformGrid>
                </StackPanel>
            </Border>

            <!-- Controller Info & Actions -->
            <Border Style="{StaticResource CardStyle}">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>
                    
                    <StackPanel VerticalAlignment="Center">
                        <TextBlock Text="Informazioni Controller" Style="{StaticResource SectionHeaderStyle}"/>
                        <StackPanel Orientation="Horizontal">
                            <TextBlock Text="Data attuale: " Style="{StaticResource LabelStyle}"/>
                            <TextBlock Text="{Binding ControllerDate}" Foreground="White" FontSize="14" FontWeight="Bold"/>
                        </StackPanel>
                    </StackPanel>

                    <StackPanel Grid.Column="1" Orientation="Horizontal">
                        <Button Content="Sincronizza Ora" Command="{Binding UpdateDateCommand}" Style="{StaticResource ActionButton}" Margin="0,0,10,0"/>
                        <Button Content="Aggiorna Dati" Command="{Binding RefreshCommand}" Style="{StaticResource ActionButton}" Margin="0,0,10,0" Background="#7f8c8d"/>
                        <Button Content="Salva Impostazioni" Command="{Binding SaveCommand}" Style="{StaticResource ActionButton}" Background="#27ae60"/>
                    </StackPanel>
                </Grid>
            </Border>
        </StackPanel>
    </ScrollViewer>
</UserControl>
